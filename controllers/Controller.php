<?php

namespace Controllers;

/**
 * The base controller for all controllers, it does not do anything but 
 * it serves as a base identifier for every controller that is used by 
 * the router.
 */
abstract class Controller
{
}
